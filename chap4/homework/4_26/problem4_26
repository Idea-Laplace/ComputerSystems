We want to set the control signal mem_write only for instructions
that write data to memory, Write HCL code for the signal mem_write in SEQ.

bool mem_write = icode in { IRMMOVQ, IPUSHQ, ICALL };

// bool mem_read = icode in { IMRMOVQ, IPOPQ, IRET };
